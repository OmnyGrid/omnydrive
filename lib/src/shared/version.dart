/// The OmnyDrive package version, surfaced by the `GET /version` endpoint of
/// both the hub and the endpoint content server. Keep in sync with
/// `pubspec.yaml`.
const String omnyDriveVersion = '1.0.0';
